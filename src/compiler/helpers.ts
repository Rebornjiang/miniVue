// 编译所需要的帮助方法

import { ASTElement } from '@type/compiler'

export function getBindingAttr (el:ASTElement, name: string, getStatic?:boolean):string | undefined {
  return undefined
}

export function getAndRemoveAttr (el: ASTElement, name: string, removeFromMap?:boolean):string | undefined {
  return undefined
}
